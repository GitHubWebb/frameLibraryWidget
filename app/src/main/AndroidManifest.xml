<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.wwx.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.framelibrary.config.FrameLibBaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.MainSelectPopActivity">

        </activity>
        <activity android:name=".view.SplitEditActivity"></activity>

        <activity android:name=".view.selectpop.JsonDataActivity" />
        <activity android:name=".view.selectpop.FragmentTestActivity">


        </activity>
        <activity android:name=".view.selectpop.TestCircleWheelViewActivity" />
    </application>

</manifest>